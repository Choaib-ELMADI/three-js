void main() {
    // Instructions

                         R    G    B  Opacity
    gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0);
}


The role of a fragment shader is to calorize 
vertices in the mesh they form